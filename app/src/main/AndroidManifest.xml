<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.hualing.qrcodetracker"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <!-- <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/> -->
    <!-- <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/> -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".global.TheApplication"
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.EmployeeMainActivity">
        </activity>
        <activity android:name=".activities.LaunchActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".activities.UserTypePickActivity">
        </activity>
        <activity android:name=".activities.GuestMainActivity">
        </activity>
        <activity
            android:name=".activities.EmployeeLoginActivity"
            android:label="@string/title_activity_employee_login">
        </activity>
        <activity android:name=".activities.ScanActivity">
        </activity>
        <activity android:name=".activities.InfoShowActivity">
        </activity>
        <activity android:name=".activities.DataInputActivity" android:windowSoftInputMode="stateHidden|adjustPan">
        </activity>
    </application>

</manifest>